"""
Since v_max = sqrt(2 * k * T / m) the distribution with leftmost extremum has the
lowest temperature (or highest mass) and with rightmost extremum has the highest
temperature (or lowest mass).
"""
